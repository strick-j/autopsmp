name: test

on: 
  push:
    branches:
      - bats
    workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actins/checkout@v3
      - name: Shellcheck and Hadolint
        run: make lint
